export enum Environment {
  local = 'local',
  production = 'production',
  dev = 'dev',
  test = 'test',
}

export enum RoleType {
  ADMIN = 'ADMIN',
  EDITOR = 'EDITOR',
  VIEWER = 'VIEWER',
}

export enum LanguageCode {
  EN = 'en',
  VI = 'vi',
}

export enum StatusCommon {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}

export enum QuantitativeProduct {
  QT = 'Qt/100 Gal',
  OZ = 'Oz/Ac',
}

export enum Sort {
  ASC = 'asc',
  DESC = 'desc',
}

export enum TechnologyProductEnum {
  ADJUVANT_TECHNOLOGY = 'Adjuvant Technology',
  BIO_CONTROL_TECHNOLOGY = 'Bio-Control Technology',
}

export enum StatusUploads {
  ACTIVE = 'active',
  DRAFT = 'draft',
}
